[build]
cmds = [
  "rm -rf node_modules/.cache 2>/dev/null || true",
  "npm install --legacy-peer-deps --no-audit",
  "cd client && npm install --legacy-peer-deps --no-audit && npm run build"
]

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"
